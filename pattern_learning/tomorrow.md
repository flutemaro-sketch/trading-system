# 明日への引き継ぎ
更新日: 2026-05-19

---

## 2日間でやったこと

### コード実装（完了・コミット済み）
- `CandleBuilder` — 5分足OHLCV自動組み立て
- `PennantDetector` — ペナントブレイク検知（要パラメータ修正）
- `AlertPopup` — ポップアップ + アラート音
- `MainWindow._process_data` に全パイプライン統合

### パターン学習（分析完了・コード未修正）
- 6枚のスクショを分析（ソフトバンク×2、三菱重工、キオクシア、スクエニ、任天堂）
- 詳細: `pattern_learning/analysis/pennant_analysis_20260519.md`

---

## 明日やること

### 優先度 高

#### 1. PennantDetector コード修正
**判定ロジック変更（最重要）:**
```python
# 現在（間違い）
if h_slope >= 0:
    return None

# 修正後（3パターン対応）
if h_slope >= l_slope:   # 収束していなければ弾く
    return None
if l_slope <= 0:         # 下値は必ず切り上がり
    return None
```

**パラメータ変更:**
| パラメータ | 現在値 | 修正値 |
|-----------|--------|--------|
| `max_candles` | 25 | 50 |
| `r2_threshold` | 0.35 | 0.25 |
| `width_shrink` | 0.15 | 0.35 |

#### 2. test_ui.py でポップアップ動作確認
- 上昇三角形ダミーデータ → アラート発報確認
- 上昇ウェッジダミーデータ → アラート発報確認

### 優先度 中
3. スクショ追加収集（まだ送るとのこと）

---

## 検知すべき3パターン

| パターン | 上値ライン | 下値ライン |
|---------|-----------|-----------|
| 上昇三角形 | フラット（slope ≈ 0） | 切り上がり |
| 対称ペナント | 切り下がり（slope < 0） | 切り上がり |
| 上昇ウェッジ | 緩やか上昇（slope > 0） | 急上昇 |
| **共通条件** | **h_slope < l_slope** | **l_slope > 0** |

## 検知不要パターン
- 下駄GTA（日足）→ 前日検討システムの領域。ペナントは作らない

---

## システム全体像（確立）
```
前日検討システム（初代秀長担当・完成済み）
  → 日足で下駄GTAパターン発見
  → 翌日の攻撃候補にリストアップ
        ↓
板監視システム（現秀長担当）
  → 5分足でペナントブレイクを検知
  → エントリータイミングをポップアップ通知
```
